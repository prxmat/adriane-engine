{
  "name": "@adriane-ai/agents-core",
  "private": true,
  "license": "Apache-2.0",
  "version": "1.4.0",
  "description": "[DEPRECATED — execution moved to the Rust engine in crates/; the TS engine remains only as a fallback when the @adriane-ai/napi native addon is absent] Agent patterns over the runtime: ReAct, plan-execute, reflection, self-correction, supervisor, swarm. Superseded by the crates/agents-core Rust crate.",
  "type": "module",
  "main": "./dist/packages/agents-core/src/index.js",
  "types": "./dist/packages/agents-core/src/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/packages/agents-core/src/index.d.ts",
      "default": "./dist/packages/agents-core/src/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@adriane-ai/artifact-store": "workspace:*",
    "@adriane-ai/graph-core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^3.2.6"
  }
}
