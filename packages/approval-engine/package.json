{
  "name": "@adriane/approval-engine",
  "private": true,
  "license": "Apache-2.0",
  "version": "0.0.1",
  "description": "[DEPRECATED — execution moved to the Rust engine in crates/; the TS engine remains only as a fallback when the @adriane/napi native addon is absent] Human-approval gates and Ed25519 attestation for sensitive actions. Superseded by the crates/approval-engine Rust crate.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@adriane/artifact-store": "workspace:*",
    "@adriane/graph-core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^2.1.9"
  }
}
