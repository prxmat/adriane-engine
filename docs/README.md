# Documentation

📖 **The Adriane documentation is now a full site:**
**https://prxmat.github.io/adriane-engine/**

Source for the site lives in [`../docs-site/`](../docs-site) (Docusaurus) — edit there.

The earlier markdown tutorials, architecture and CLI notes that lived here have
been migrated into the site, with explicit TypeScript **and** Python examples and
up-to-date package names (`@adriane-ai/graph-sdk`, `@adriane-ai/cli`,
`pip install adriane-ai` / `import adriane_ai`).

Architectural decision records remain under [`adr/`](./adr).
