name: coverage

# JS/TS test coverage (vitest + @vitest/coverage-v8 provider) across the
# workspace, with the HTML/JSON report uploaded as a build artifact.

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: coverage-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

jobs:
  coverage:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - uses: dtolnay/rust-toolchain@1.96.0
      - run: pnpm install --frozen-lockfile
      # The SDK is Rust-only (no TS fallback): tests run graphs on the native engine, so
      # build the napi addon first — `@adriane-ai/napi` loads it from crates/bindings.
      - name: Build napi addon
        working-directory: crates/bindings
        run: pnpm exec napi build --platform
      # @adriane-ai/verify imports the @adriane-ai/graph-sdk BUNDLE (dist), not its source — so its
      # vitest project needs the bundle present. The unit job gets it via turbo's ^build; the raw
      # `vitest run --coverage` here does not, so build it explicitly before the coverage run.
      - name: Build graph-sdk bundle (resolved by @adriane-ai/verify tests)
        run: pnpm --filter @adriane-ai/graph-sdk build
      - name: Run tests with coverage
        run: pnpm test:coverage
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: coverage-report
          path: coverage/
          if-no-files-found: warn
