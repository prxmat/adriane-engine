{
  "name": "@adriane/plugin-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "stdio MCP server that runs the Adriane engine (@adriane/graph-sdk) in-process — execute governed agents/graphs with the real suspend-on-sensitive-tool / resume-after-approval loop — plus validate_graph + compile_graph_yaml via @adriane/napi.",
  "license": "Apache-2.0",
  "main": "server.mts",
  "scripts": {
    "smoke": "node --import tsx smoke.mts"
  },
  "dependencies": {
    "@adriane/approval-engine": "workspace:*",
    "@adriane/graph-sdk": "workspace:*",
    "@adriane/napi": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "tsx": "^4.20.6"
  }
}
